Entity এবং Attribute

Entity হলো সিস্টেমে ট্র্যাক/ম্যানেজ করা কোনো জিনিস।

আর সেই Entity-এর বৈশিষ্ট্য সংরক্ষণের জন্য Attribute ব্যবহার করা হয়।

প্রতিটি Attribute মূলত ডেটাবেজে সেই Entity-এর তথ্য জমা রাখে।

Attribute-এর ধরন
1. Simple Attribute (সহজ/অখণ্ড বৈশিষ্ট্য)

যেগুলোকে আর ভাঙা যায় না।

এগুলো সরাসরি একক মান (single value) ধরে।
👉 উদাহরণ:

Student → Age (বয়স)

Employee → Gender (লিঙ্গ)

Product → Price (দাম)

2. Composite Attribute (যৌগিক বৈশিষ্ট্য)

যেগুলোকে ছোট ছোট অংশে ভাগ করা যায়।

মানে এক Attribute-এর ভেতর আরও একাধিক উপ-Attribute থাকে।
👉 উদাহরণ:

পূর্ণ নাম (Full Name) → প্রথম নাম (First Name) + শেষ নাম (Last Name)

Address → House No + Road No + City + Country

Date → Day + Month + Year


Entity এবং Database এর সম্পর্ক

ডাটাবেজ (Database) মূলত Entity এবং তাদের Attribute সংরক্ষণের জায়গা।

👉 সহজ করে বললে:

Entity → Table (টেবিল) আকারে সংরক্ষণ করা হয়

Entity-এর Attribute → Column (Field)

Entity-এর প্রতিটি আলাদা অবজেক্ট → Row (Tuple)

উদাহরণ: Student Entity

Entity: Student
Attributes: Student_ID, Name, Age, Class

Student_ID	Name	Age	Class
1	Rahim	15	9
2	Karim	16	10
3	Ayesha	14	8

এখানে Student হলো Entity → যা ডাটাবেজে একটা Table।

টেবিলের প্রতিটি Column (Student_ID, Name, Age, Class) → Attribute।

প্রতিটি Row (Rahim, Karim, Ayesha) → আলাদা আলাদা Student Entity।

Key Point (মনে রাখার জন্য)

Database = Entity এবং Attribute রাখার জায়গা।

Entity = Database Table

Attribute = Table Column

Row (Tuple) = প্রতিটি আলাদা Entity instance

✅ এক লাইনে মনে রাখুন:
Entity → Table, Attribute → Column, Row → Entity-এর Instance







🟢 Strong Entity (শক্তিশালী)

ধরুন, একটা স্কুল আছে।
সেখানে Student একটা Entity।

প্রতিটি Student-এর একটা Student_ID আছে (Roll Number এর মতো)।

আপনি শুধু Student_ID দিয়েই একজন Student-কে চেনাতে পারবেন।
👉 মানে Student নিজে নিজে দাঁড়াতে পারে → তাই Strong Entity।

উদাহরণ (Table):

Student_ID (PK)	Name	Age
1	Rahim	15
2	Karim	16
🔴 Weak Entity (দুর্বল)

এখন ভাবুন, প্রত্যেক Student-এর Exam Result রাখতে হবে।
কিন্তু Problem হলো → শুধু "Math - 80 Marks" লিখলে বোঝা যাবে না এই Result কার।

👉 তাই এই Result Entity Student ছাড়া দাঁড়াতে পারে না।
👉 Result কে চেনাতে হলে লাগবে:

Student_ID (Strong Entity এর Key) + Subject_Name

উদাহরণ (Table):

Student_ID (FK)	Subject	Marks
1	Math	80
1	English	75
2	Math	90

এখানে Exam Result Weak Entity, কারণ—

এর নিজের কোনো Primary Key নেই।

ওকে আলাদা করতে Student_ID (Strong Entity) এর উপর নির্ভর করতে হচ্ছে।

🧠 সহজভাবে মনে রাখুন:

Strong Entity = নিজের আইডি (Primary Key) আছে → স্বাধীনভাবে চেনা যায়।

Weak Entity = নিজের আলাদা আইডি নেই → Strong Entity ছাড়া চেনা যায় না।

👉 একটা ছোট ট্রিক:

যদি Entity টেবিলে নিজস্ব Primary Key থাকে → Strong Entity।

যদি Entity টেবিলে Primary Key বানাতে হয় Strong Entity-এর Key নিয়ে → Weak Entity।